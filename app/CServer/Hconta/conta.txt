Menú principal:
  annotations[fast normal]
  plan
  reports
  cofiguration
  close

Plan:
Group
Subgroup
count (2 digits)
Simple list

SIMPLE LIST
===========
Field:
  "s:text" string,
  "i:path" image,
  "c:true|false" checkbox,
  "n:number",
  "d:serial Dec"
VALUE = Array<string>
LIST = Array<VALUE>
Model:
  values: LIST
  validate: (LIST, VALUE) -> String (error)
  add: (VALUE)->LIST
  modify: (Int, VALUE)->LIST
  del: (Int)->LIST

View:
  parent
  widths (only for s: n: and d:. Other cases this prameter is unused; but
          must be set -e.g. 0)

editedIx (-1, 0...)


